import React from "react";
import { features } from "../constants";
import styles, { layout } from "../style";
import Button from "./Button";

const Business = () => {
  return <section className={layout.section} id="features" >
    <div></div>
  </section>;
};

export default Business;
